<% provide(:title, @user.username) %>

<% content_for :sidebar do %>
  <%= render 'users/sides/show_sidebar' %>
<% end %>

<div class="span6">
  <h2>Goals</h2>

    <%= render :partial => @user.goals, locals: { make_edits: false } %>
  <hr>
</div>
