<%= form_for @comment, url: {action: "update", controller: "comments"} do |f| %>

  <%= f.label :body, "Comment" %><br/>
  <%= f.text_area :body %>

  <%= f.submit "Update comment", class: "btn btn-large btn-primary" %>
  <%= link_to 'Cancel', post_path(@post), class: "btn btn-large" %>

<% end %>